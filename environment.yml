name: tamp-tut
channels:
  - conda-forge
dependencies:
  - python=3.10
  - jupyterlab
  - ipywidgets
  - pygrib>=2.1.5  # macos m-series chip installation is problematic through pip
  - pip
  - pip:
    - -e .
